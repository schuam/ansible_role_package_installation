---
install_bluetooth_packages: true
install_browser_packages: true
install_essential_packages: true
install_multimedia_packages: true
install_network_packages: true
install_python_packages: true
install_x_related_packages: true

# The following variables are relevant for Ubuntu
# Usually I would say that if you want to install the browser and/or essential
# packages, brower snap and/or essetial snap packages should be installed as
# well. But I'm not sure, if I want to install snap packages on all my systems,
# therefore I let the choice to do so in here.
install_browser_snap_packages: true
install_essential_snap_packages: true
